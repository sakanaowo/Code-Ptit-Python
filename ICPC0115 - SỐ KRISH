from math import *


def check(n):
    sum=0
    for i in range(len(n)):
        sum += factorial(int(n[i]))
    return  sum==int(n)

for case in range(int(input())):
    n = input()
    print("Yes" if check(n) else "No")
